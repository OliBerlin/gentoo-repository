--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -220,6 +220,10 @@
 # FFmpeg libs
+# Use system FFmpeg via pkg-config / find_package
+find_package(FFMPEG REQUIRED)
+
+# Remove hardcoded Ubuntu paths
-set(FFMPEG_LIBS
-    /lib/libavcodec.so
-    /lib/libavformat.so
-    /lib/libavutil.so
-    /lib/libswscale.so
-    /lib/libswresample.so
-)
+set(FFMPEG_LIBS ${FFMPEG_LIBRARIES})
